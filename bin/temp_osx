#!/bin/bash

function temp() {
  istats ${@} --no-graphs --no-labels | tr -d '[:space:]'
}

gpu=$(temp 'extra TG0D')
cpu=$(temp 'cpu temp')
pipe=$(temp 'extra Th1H')

echo -en "HeatPipe: ${pipe}   GPU: ${gpu}   CPU: ${cpu}"

